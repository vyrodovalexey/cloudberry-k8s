<?xml version="1.0" encoding="UTF-8"?>
<?xml-stylesheet type="text/xsl" href="configuration.xsl"?>
<!--
  Hive configuration for the test Metastore + HiveServer2.

  The Metastore Thrift endpoint (thrift://hive-metastore:9083) matches the PXF
  server config in specifications/12-data-loading-spec.md so PXF `hive:*`
  profiles resolve against this test metastore. Metadata is persisted in the
  dedicated `hive-metastore-db` Postgres instance.
-->
<configuration>
  <!-- Metastore backing database (Postgres). -->
  <property>
    <name>javax.jdo.option.ConnectionURL</name>
    <value>jdbc:postgresql://hive-metastore-db:5432/metastore</value>
  </property>
  <property>
    <name>javax.jdo.option.ConnectionDriverName</name>
    <value>org.postgresql.Driver</value>
  </property>
  <property>
    <name>javax.jdo.option.ConnectionUserName</name>
    <value>hive</value>
  </property>
  <property>
    <name>javax.jdo.option.ConnectionPassword</name>
    <value>hive</value>
  </property>

  <!-- Metastore Thrift service binding. -->
  <property>
    <name>hive.metastore.uris</name>
    <value>thrift://hive-metastore:9083</value>
  </property>
  <property>
    <name>hive.metastore.port</name>
    <value>9083</value>
  </property>

  <!-- Warehouse root on the test HDFS. -->
  <property>
    <name>hive.metastore.warehouse.dir</name>
    <value>hdfs://namenode:8020/user/hive/warehouse</value>
  </property>
  <property>
    <name>metastore.warehouse.dir</name>
    <value>hdfs://namenode:8020/user/hive/warehouse</value>
  </property>

  <!-- Auto-create/upgrade the metastore schema in the test env so no manual
       schematool step is needed (the setup script also runs schematool). -->
  <property>
    <name>datanucleus.schema.autoCreateAll</name>
    <value>true</value>
  </property>
  <property>
    <name>hive.metastore.schema.verification</name>
    <value>false</value>
  </property>

  <!-- HiveServer2 (Thrift JDBC on :10000) for ad-hoc SQL in tests. -->
  <property>
    <name>hive.server2.thrift.port</name>
    <value>10000</value>
  </property>
  <property>
    <name>hive.server2.thrift.bind.host</name>
    <value>0.0.0.0</value>
  </property>
  <property>
    <name>hive.server2.enable.doAs</name>
    <value>false</value>
  </property>
  <property>
    <name>hive.server2.active.passive.ha.enable</name>
    <value>false</value>
  </property>
</configuration>
